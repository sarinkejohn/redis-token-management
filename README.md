# redis-token-management
A Spring Boot 3 REST API implementing JWT-based authentication with multi-device session support using Redis. Supports user registration, login, logout, and token refresh. Designed with security best practices, including refresh token validation, token blacklisting, and hashed token storage in Redis.
